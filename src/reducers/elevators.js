// @flow
/**
 * @module Reducers/AdvertReports
 * @desc AdvertReports Reducer
 */

import { handleActions } from 'redux-actions';

const initialState = {
};

export default handleActions({
}, initialState);
